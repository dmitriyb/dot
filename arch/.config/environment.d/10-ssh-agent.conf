# Point graphical-session consumers (non-fish GUI apps) at the systemd-managed agent.
# Fish sets this itself too (shared/.config/fish/config.fish) as the primary mechanism;
# this file just covers apps launched directly by the systemd user manager.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
