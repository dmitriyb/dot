# SHA-256 of the application suffix (the part after `id_<type>_sk_rk_`) of resident
# FIDO2 keys that are sign-only / no-touch. These keys are:
#   - excluded from the main ssh-agent by `ssh-load-keys`
#   - the only key loaded into the ephemeral agent by `docker-claude --agent`
# Only hashes are stored here, never the key name. One hex digest per line.
# Add a key:  printf '%s' '<app-suffix>' | sha256sum
198a108f96a804e16ad06573ced8069f821931b94631f081317dce9ef2fe0ee0
